// tuple is a typed Array with a fixed number of elements where types
// of the elements are known.


let myTuple:[number,string]=[1,"Naveed"];


/* 
In Tuple, we can add all data types in a single array by following the above syntax 
but in Arrays We can only define one data type into one array.
The only drawback of tuple is that we have to add fix number of elements with 
specific order. 
*/

// let name:[string,number]=[1,"naveed"] // Error: sequence is not followed 
// let name1:[string,number]=["naveed",1,2] // Error: extra element is added. 

// Storing person name with `\t` and `\n` and whiteSpace in the end.
let person = "\tFirstName: Naveed \nLastName:  Khalid ";
console.log(`Name with WhiteSpace:\n ${person}`);
console.log(`Name without WhiteSpaces:\n${person.trim()}`);
export {};
